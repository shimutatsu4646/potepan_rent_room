<%= form_with model: @reservation, url: {action: 'confirm'} do |f| %>
  <!--ここにエラー文入れる-->
  <!--innのpriceを表示、合計金額はhiddenで渡す？-->
  <!--とりあえずconfirmアクション内で、newの入力値からtotal_priceを計算して代入する方法でやる。-->
  <%= f.label :start_time %>
  <%= f.date_field :start_time %>
  <%= f.label :finish_time %>
  <%= f.date_field :finish_time %>
  <%= f.label :number %>
  <%= f.date_field :number %>
  <!-- f.hidden_field :user_id, value: current_user.id -->
  <!--current_user.idを実装する必要あり。-->
  <!-- f.hidden_field :inn_id, value: @inn.id -->
  <!--@innはshowから引っ張ってくる-->
  <%= f.submit "確認画面へ" %>
<% end %>
